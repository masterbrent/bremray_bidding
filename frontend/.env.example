# Frontend Environment Variables Example

# API URL Configuration
# For development with backend on same machine: /api (uses Vite proxy)
# For development with separate backend: http://backend-host:port/api
# For production: /api (when served from same domain) or https://api.yourdomain.com/api
VITE_API_URL=/api
